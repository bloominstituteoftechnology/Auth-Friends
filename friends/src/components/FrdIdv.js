import React from 'react'

const FrdIdv = (props) => {
  return (
    <div>
      <h2>{props.name}</h2>
      <h4>{props.age}</h4>
      <h4>{props.email}</h4>
    </div>
  )
}

export default FrdIdv;
