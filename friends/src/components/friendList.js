import React, { useState } from 'react'

const friendList = () => {
    const [ friends, setFriends ] = useState([]);
    
    return (
        <div>

        </div>
    )
};

export default friendList;