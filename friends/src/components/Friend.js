import React from 'react';

const Friend = (props) => {
    return(
        <div>
            <h3>{props.friend.name}</h3>
            {/* <h2>this is working now...</h2> */}
        </div>
    )
}

export default Friend;