
const initialState = {
    postingFriend: false,
    fetchingFriends: false,
    error: null,
    friends: [],
};

export default (friendsReducer = (state = initialState, action) => {
    return state;
});