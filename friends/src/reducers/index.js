import {
	FETCHED_FRIENDS,
	FETCHING_FRIENDS,
	ERROR
} from "../actions/fetchFriends";

const initialState = {};

export const friendsReducer = (state = initialState, action) => {
	switch (action.type) {
		case "":
			return;
		case "":
			return;
		case "":
			return;
		default:
			return state;
	}
};
