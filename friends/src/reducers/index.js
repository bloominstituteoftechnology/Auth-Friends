
const initialState = {
    postingFriend: false,
    fetchingFriends: false,
    error: null,
    friends: [],
};

const friendsReducer = (state = initialState, action) => {
    return state;
};

export default friendsReducer;