import {getFriends, createFriend} from '../actions';

const initialState = {
    selectedFriend: {},
}

export const friend = (state = initialState, action) => {
    switch(action.type) {
  
  
        default: 
          return state;
    }
  }