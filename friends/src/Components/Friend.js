import React,{Component} from 'react'

const Friend = (props) => {

    return (
        <div>
            <h3> {props.name} </h3>
            <h3> {props.}
        </div>
    )
}

export default Friend